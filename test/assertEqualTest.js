const assertEqual = require('../assertEqual');

// TEST CODE
assertEqual("Lighthouse Labs", "Bootcamp");
assertEqual(1, 1);
assertEqual("Lighthouse Labs", "Lighthouse Labs" );
assertEqual(8, 1);
assertEqual(9, 9);

//use node test/assertEqualTest.js in lotide directory (not in test subdirectory)